# 燃點品牌設計 · 後台部署手冊

> 目标：让你能登录 `admin.ip-design.hk`，在后台**新增、编辑、删除作品案例**，无需碰代码。
> 全程预计 **60-90 分钟**（含等待时间）。

---

## 准备阶段（5 分钟）

确认你手上有：
- [x] Sanity 账号（已注册，Project ID: `6fxw2dmo`）
- [x] Cloudflare 账号（已在用）
- [x] 域名 `ip-design.hk`（已绑定）
- [ ] GitHub 账号（**这步要新注册**）

---

## 第一阶段 · 配置 Sanity 允许网站访问（5 分钟）

这步必须做，否则网站取不到 Sanity 的数据。

### ▶ 打开
👉 https://www.sanity.io/manage/personal/project/6fxw2dmo/api

### ▶ 在 **CORS Origins** 区域，依次添加 3 个网址：

1. 点 **"Add CORS origin"**
2. **Origin:** `https://ip-design.hk` → **Allow credentials: 不勾** → Save
3. 再点一次，添加：`https://www.ip-design.hk` → 不勾 → Save
4. 再点一次，添加：`https://admin.ip-design.hk` → **勾选 Allow credentials** → Save

**截图给我确认 3 条都加好了。**

---

## 第二阶段 · 注册 GitHub（10 分钟）

GitHub 是放代码的地方，免费，全球开发者都用它。

### ▶ 注册
1. 打开 https://github.com/signup
2. 用邮箱 `baoguangwen7708@gmail.com` 注册
3. 设密码、用户名（建议 `ipdesign-hk` 或类似）
4. 验证邮箱

### ▶ 创建仓库（Repository）
1. 登录后右上角点 **`+`** → **New repository**
2. **Repository name:** `ip-design-hk`
3. **Public** （选 Public 不影响隐私，代码本身无密码）
4. 不要勾选 README / .gitignore / license
5. 点 **Create repository**

进入新仓库的空白页，**保留这个页面**。

---

## 第三阶段 · 上传代码到 GitHub（15 分钟）

### ▶ 下载我给你的代码包

我会另外提供一个 **整站打包.zip** 文件。下载后**解压**得到一个文件夹。

文件夹里面应该有这些（注意 `studio/` 是子文件夹）：
```
index.html
styles.css
app.jsx
visuals.jsx
sections-a.jsx
sections-b.jsx
tweaks-panel.jsx
studio/
  package.json
  sanity.config.js
  schemas/
  ...
assets/
```

### ▶ 上传

1. 回到 GitHub 仓库空白页
2. 点页面中间 **"uploading an existing file"** 蓝色链接
3. **把解压后文件夹里的所有内容**（注意是文件夹**里面**的东西，不是文件夹本身）**全选拖进上传框**
4. 等所有文件上传完
5. 页面底部 **Commit changes** → 点绿色按钮

> ⚠️ 如果文件太多上传失败 — 可以分两次：先上传根目录的文件，再单独上传 `studio` 文件夹

---

## 第四阶段 · 用 GitHub 自动部署主网站（15 分钟）

把 `ip-design.hk` 主网站从手动上传改成 **GitHub 自动部署**，以后改内容只要 push 就自动更新。

### ▶ 在 Cloudflare 创建新 Pages 项目

1. 进 Cloudflare → Workers and Pages → **Create application**
2. 选 **Pages** 标签 → 选 **Connect to Git**
3. 第一次会让你**授权 Cloudflare 访问 GitHub** → 同意
4. 选刚才创建的仓库 **ip-design-hk**
5. 项目名填：`ip-design-main`

### ▶ 构建设置（关键！）

| 字段 | 填什么 |
|---|---|
| **Production branch** | `main` |
| **Framework preset** | None（无） |
| **Build command** | （留空） |
| **Build output directory** | `/` |
| **Root directory** | （留空） |

点 **Save and Deploy** → 等 1-2 分钟构建完成。

### ▶ 绑定主域名

1. 进 `ip-design-main` 项目 → Custom domains
2. 添加 `ip-design.hk` 和 `www.ip-design.hk`
3. 如果提示"域名已被其他项目占用" → 先删旧项目（`calm-lab-f0c7`）的域名绑定，再加到新项目

---

## 第五阶段 · 部署 Sanity 后台（20 分钟）

### ▶ 在 Cloudflare 再创建一个 Pages 项目（专门给后台）

1. Cloudflare → Workers and Pages → **Create application**
2. **Pages** → **Connect to Git**
3. 选**同一个仓库** `ip-design-hk`（没错，同一个仓库）
4. 项目名填：`ip-design-studio`

### ▶ 构建设置（专给后台）

| 字段 | 填什么 |
|---|---|
| **Production branch** | `main` |
| **Framework preset** | None |
| **Build command** | `npm install && npm run build` |
| **Build output directory** | `studio/dist` |
| **Root directory (advanced)** | `studio` ⚠️ 一定要填 |

> ⚠️ "Root directory" 在 Cloudflare 后台叫 **"Root directory"** 或 **"Build system"** 里的 **"Root directory (advanced)"**，要把范围限制在 `studio/` 子文件夹

点 **Save and Deploy** → 第一次构建会比较慢（5-10 分钟），它要下载 Sanity 的依赖包。

### ▶ 绑定后台域名

1. 构建成功后，进 `ip-design-studio` 项目
2. Custom domains → Set up a custom domain
3. 输入 **`admin.ip-design.hk`**
4. Cloudflare 自动配 DNS 和 HTTPS

---

## 第六阶段 · 测试后台（5 分钟）

### ▶ 打开后台
👉 https://admin.ip-design.hk

第一次打开会让你**用 Sanity 账号登录**（就是你注册 Sanity 时的邮箱）。

### ▶ 登录后应该看到
- 左侧菜单：**「案例 · 作品集」**
- 右侧：空白列表（还没有内容）
- 右下角："Create new" 按钮

### ▶ 试着发第一个案例

1. 点 **"Create new"**
2. 填案例标题：例如 `测试案例`
3. 填描述：例如 `这是第一个测试案例`
4. 选分类：例如 `IP / 潮玩`
5. 填年份：`2025`
6. 上传一张封面图（拖入即可）
7. 选卡片尺寸：例如 `大卡`
8. 点右下角 **"Publish"** 发布

### ▶ 验证
打开 `https://ip-design.hk` → 滚动到「服務案例」区 → **你刚发的测试案例应该出现在最前面** ✓

---

## 日常使用 · 怎么发新作品

### 增加案例
1. 登录 https://admin.ip-design.hk
2. 点「案例 · 作品集」→ Create new
3. 填好资料 → Publish
4. 几秒后 `ip-design.hk` 自动更新 ✓

### 编辑案例
1. 后台 → 点案例标题进入编辑页
2. 改内容 → Publish
3. 网站自动更新

### 删除案例
1. 进入案例编辑页
2. 右上角 "..." → Delete

### 暂时下架（不删除，只是不显示）
1. 进入案例编辑页
2. 右上角 "..." → Unpublish

---

## 常见问题

### Q1. admin.ip-design.hk 显示 502 / 错误
**原因：** 第五阶段 Cloudflare 构建失败。
**处理：**
1. 进 `ip-design-studio` 项目 → Deployments
2. 看最新一次构建的日志，截图给我

### Q2. 后台能登录，但发的案例在网站上不显示
**原因：** 第一阶段 Sanity CORS 没配好。
**处理：** 回到 https://www.sanity.io/manage/personal/project/6fxw2dmo/api 检查 CORS Origins 有没有 `https://ip-design.hk` 和 `https://www.ip-design.hk`。

### Q3. 想再加新模块（客户列表 / 价格 / 动态）
告诉我加哪个，我把 schema 文件给你 → 你 push 到 GitHub → Cloudflare 自动重新构建 → 后台多一个菜单。

### Q4. 30 天后 Sanity Growth Trial 到期
正常 — 你的内容不会丢，只是会**自动降到 Free 免费版**。
Free 版限制：3 个用户、10GB 资产、每月 100k API 请求 — 对你来说完全够用。

---

## 我能帮你的事

任何一步卡住、截图发我。
- 看不懂英文界面 → 我翻译
- 构建失败 → 看日志我排查
- 字段不够用 → 加 schema

后台跑通后，下一步可以：
- 加 **客户列表** 模块（让客户墙变成可编辑）
- 加 **动态 / 新闻** 模块
- 加 **联系方式** 模块（地址、电话也能在后台改）

跟我说哪个先做，我陆续加。
