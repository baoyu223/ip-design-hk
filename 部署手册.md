# 上线手册 · ip-design.hk

> 目标：把当前网站上线到 `ip-design.hk`，全程免费，1 小时内完成。

---

## 准备工作

请确认你手上有：
- [ ] 域名 `ip-design.hk` 的注册账号（在哪买的就登录哪里）
- [ ] 一个邮箱（用来注册 Cloudflare）

---

## 第 1 步 · 注册 Cloudflare（5 分钟）

1. 打开 https://dash.cloudflare.com/sign-up
2. 用邮箱注册账号，验证邮箱
3. 登录进去后，点左上角 **"Add a site"** 或 **"添加站点"**
4. 输入 `ip-design.hk` → 选 **Free 免费方案** → 继续

Cloudflare 会扫描你域名的现有 DNS 记录（可能为空，没关系）。

最后它会给你**两个 Name Server 地址**，长得像：
```
xxx.ns.cloudflare.com
yyy.ns.cloudflare.com
```
**把这两个地址抄下来或截图保存。**

---

## 第 2 步 · 把域名"指向"Cloudflare（10 分钟，等待 1-24 小时）

去你买域名的网站登录（如阿里云 / GoDaddy / Namecheap / HKDNR）。

找到 `ip-design.hk` 的管理页面，找一项叫：
- **DNS 服务器** / **Name Servers** / **NS 设置**

把里面的两条记录**改成**第 1 步抄下来的两个地址。

保存。

> **提醒：** 这一步生效时间从 10 分钟到 24 小时不等，通常 1 小时内就好。Cloudflare 后台会显示 "Active 活跃" 状态时即生效。

---

## 第 3 步 · 部署网站到 Cloudflare Pages（10 分钟）

1. 在 Cloudflare 后台，左侧菜单点 **"Workers & Pages"**
2. 点 **"Create"** → 选 **"Pages"** → 选 **"Upload assets"**
3. 项目名填：`ip-design` （随便起一个）
4. **把我打包好的 zip 文件拖进上传框**
5. 等几秒上传完，点 **"Deploy site 部署"**

部署完后 Cloudflare 会给你一个临时网址，比如：
```
https://ip-design.pages.dev
```
**先点开看看网站显示正不正常。** 如果正常，进入第 4 步。

---

## 第 4 步 · 绑定自己的域名 ip-design.hk（5 分钟）

1. 在刚部署成功的项目页面，点上方 **"Custom domains 自定义域名"** 标签
2. 点 **"Set up a custom domain 设置自定义域名"**
3. 输入 `ip-design.hk` → 确认
4. 再点一次添加 → 输入 `www.ip-design.hk` → 确认

Cloudflare 会自动给你加好 DNS 记录、自动签 HTTPS 证书。

**5 分钟后**，打开浏览器输入 `ip-design.hk` 就能访问网站。

---

## 第 5 步（可选）· 加联系表单邮件转发

当前网站的联系表单点击"发送"只显示"已收到"的提示页，不会真的发邮件。要让表单内容自动发到你邮箱：

### 方案 A · Formspree（最简单，免费 50 封/月）

1. 去 https://formspree.io 注册账号（用你的 gmail）
2. 新建一个 form，绑定 `baoguangwen7708@gmail.com`
3. Formspree 会给你一个地址，类似 `https://formspree.io/f/xxxxxxx`
4. 把这个地址发给我，我帮你接入到网站表单

### 方案 B · 升级到企业邮箱（推荐）

注册一个 `hello@ip-design.hk` 或 `contact@ip-design.hk` 邮箱，更专业。
推荐 **Zoho Mail**（5 个账户免费）或 **腾讯企业邮箱**。

---

## 以后怎么更新网站？

**有两种方式：**

### 方式 1 · 每次手动上传（适合改动少时）
- 改完文件后，重新打包 zip
- 在 Cloudflare Pages 项目里点 **"Create new deployment"**
- 拖入新 zip → 上传 → 自动替换

### 方式 2 · 接 GitHub 自动部署（推荐）
- 注册 GitHub 账号（github.com）
- 把项目上传到一个仓库
- Cloudflare Pages 设置里改成 **"Connect to Git"**
- 以后改完代码 push 到 GitHub → Cloudflare 自动重新部署 → 网站自动更新

如果你想走自动化路线（推荐），跟我说一声，我教你一步步设置。

---

## 进阶 · 加上"可编辑后台"（推荐 v2 阶段做）

目前网站的客户列表、案例、联系方式都写在代码里，**改一次要改代码**。

下一步可以接入 **Sanity.io** 这种"无头 CMS"，做成：
- 你登录后台 → 点"新增案例"→ 上传图、填标题 → 保存
- 网站自动更新，不用碰代码

成本：免费档（3 个用户、10GB 资产，对你完全够用）。  
改造周期：5-7 天。

跟我说"做后台"我就开始改造。

---

## 任何卡住的地方

如果你在某一步卡住了，告诉我：
- 你在哪一步（第几步）
- 看到的报错信息或截图描述
- 你点了哪个按钮

我立刻教你下一步怎么做。
